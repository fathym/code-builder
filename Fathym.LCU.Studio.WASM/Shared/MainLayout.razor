@inherits MainLayoutBase

@if (studioState != null)
{
    <div>
        <Layout Sider>
            @if (studioState.ActivityBar != null)
            {
                <LayoutSider>
                    <LayoutSiderContent>
                        <IDEActivityBar @ref="activityBar" Title="@studioState.ActivityBar.Title"
                                Icon="@studioState.ActivityBar.Icon"
                                Items="studioState.ActivityBar.Items"></IDEActivityBar>
                    </LayoutSiderContent>
                </LayoutSider>
            }

            <Layout>
                @if (studioState.Header != null)
                {
                    <LayoutHeader Fixed Class="z-index-200">
                        <IDEHeader Title="@studioState.Header.Title"
                           PackagePath="@($"studio/{studioState.CurrentPackage}")"
                           PluginPath="@studioState.CurrentPlugin"
                           Items="studioState.Header.Items"
                           BarRef="activityBar.Sidebar">
                        </IDEHeader>
                    </LayoutHeader>
                }

                <LayoutContent>
                    <Div Height="Height.Is100">
                        @Body
                    </Div>
                </LayoutContent>

                <LayoutFooter>
                    <IDEStatusBar Status=""></IDEStatusBar>
                </LayoutFooter>
            </Layout>
        </Layout>
    </div>
}

